Project-specific user instructions

- 2026-09-10 主线修订（用户选定「降级为加分项」）：主线改为纯业界复制——dense Qwen3.5-9B + slime 原生 OPD + 27B teacher + DSH；LoopLM 循环层降为限额实验臂（试点 ≤8 GPU·h，恢复 <50% dense 参照即止损封存）。E1 资产保留。此前 loopify 为主线的相关条目自此被本条覆盖。

- 2026-09-10 预算与 teacher 定案（用户确认「2-4张是ok的」）：GPU 预算由最多 2 张放宽到 2–4 张 H100 80GB（逐次授权、共享机纪律不变）；OPD teacher 定为 Qwen3.5-27B 全程唯一（E3 恢复与 E4 agent 都是它，外部 SGLang 服务、专职一张卡），dense 9B 的 96.9% 仅作恢复率参照。显存算术结论（已记录于 plan.json）：4 张卡下 27B 原版/loopify-27B 全参训练仍不可行，student 维持 loopify-9B；分工 = 1 张 teacher + 3 张训练/rollout。其余 E1–E5 计划不变。

- 2026-09-10 方向定案（用户原话「LoopLM我说的是借鉴他的思想，我们自己训练。opd可以slime原生管线」）：LoopLM 取其共享权重循环思想、模型自己训练（loopified Qwen3.5-9B，20/32 唯一层≈5.6B，复用 P1 手术/等价性验证/0% 退化基线）；OPD 一律走项目固定 slime 的原生管线（`--use-opd`，teacher 为外部 SGLang 服务），不自研 trainer。实施计划 E1–E5 与已核验挂载点、风险清单见 README loop-agent-design 段与 configs/loop-agent/plan.json。GPU 步骤仍逐次授权；官方 benchmark 协议规则继续有效。

- 2026-09-10 用户最新纠正（最高优先级，覆盖同日早前的 loopify 决策）：「我们 looplm 跟 9b 模型有什么关系？最重要的是调研，不要自己意淫。」LoopLM 指真实存在的 Ouro 族模型（arXiv 2510.25741），不是需要自建改造的东西；loopify-Qwen3.5-9B 主线废弃（其 P1 产物保留为证据）。当前优先事项为 LoopLM/OPD 深度调研，结论已写入 README loop-agent-design 段；三个候选方向（slime 原生 OPD 主线 / Ouro-2.6B 作 student / Ouro 作对照基线）待用户选择后方可实施。未选择前不得再启动新的自建工程或训练。【注：调研完成后用户已选定方向，见上条；本条保留以存证。】

- 2026-09-10 最新用户决定（最终）：本项目定位为简历面试项目，主线改为自研 LoopLM + OPD 组合：对 Qwen3.5-9B 做 RRT 式层共享手术得到 ~5.6B 有效参数的循环架构 student，以 Qwen3.5-27B 为 teacher（保留，未去掉）做两段 OPD（恢复 + agent 蒸馏），并自跑 GRPO 对比线。用户经选项确认「自研 loopify（而非用 Ouro）」与「加 GRPO 线」两个关键决策。冻结设计见 README 顶部 loop-agent-design 段与 `configs/loop-agent/plan.json`。本轮授权：下载固定 revision 的 Qwen3.5-9B 快照（19.33GB，LFS sha256 校验，存于项目内）、编写 P1 手术脚本及 CPU 侧分组/导出验证。尚未动用 GPU、未修改权重、未训练、未跑 benchmark；GPU 步骤（vLLM serving 验证、退化测量、后续训练）仍需明确授权。此前「停止自建管线」限制不再适用于上游框架无法支持的自研 loopify 必要部件（rollout 驱动、trainer），且该转向经用户明确选择；slime/DSH CPU 验证与数据排除审计继续复用，官方 benchmark 协议规则继续有效。【注：本条已被同日稍后的用户纠正覆盖，loopify 主线废弃；保留原文以存证。】

- 2026-09-09 最新用户明确要求“转这个架构吧”（指 slime）。正式工程主线切为 THUDM/slime，固定 `4c193f1f37509cca70f0e88807a9305b70f63f4e`；整体复用上游 coding_agent_rl、SGLang、Megatron 和 Ray，DSH 保留为目标 agent。此决定取代此前 verl/AGL 的默认选型与纯调研暂停，不授权新增自建训练/评分管线。保留 Qwen3.5-27B BF16 + LoRA、最多两张 H100、项目内存储与历史数据排除。当前仅完成独立 CPU 依赖、上游 agent 测试和单轮真实 DSH SDK 对原生 adapter 的脚本模型检查；尚无完整 DSH BaseHarness/项目内任务环境接入，未采用未合并 LoRA PR。`scripts/agent_rl.sh` 默认转到 slime 准备入口，`--launch` 继续拒绝；未新增 GPU rollout/参数更新授权。最新证据见 README 顶部、`configs/slime/architecture.json` 和 `runtime/slime/acceptance.json`。

- 2026-09-09：用户要求开始上游方案接入并询问首轮规模。已开始固定 Agent Lightning 官方源码和独立 CPU 依赖/接口检查；保持不自建训练管线。2 题×2 轨迹、最多 1 次更新是首轮建议预算，尚未据此启动 GPU 或参数更新。AGL 固定版本的流式不兼容已由实际 DSH 请求重放确认，不能绕过该失败启动训练或自行补建 SSE 代理。见 README 最新检查记录。

- 2026-09-09 最新用户纠正：停止扩展自建 code-agent 训练管线，优先充分调研并整体复用成熟开源方案。当前工作转为官方文档、固定源码、完整训练配方和兼容性差距的审阅；不继续自建任务批量验收/调度/轨迹/奖励框架，不因已写代码而默认保留该架构。已下载数据、依赖和 CPU 证据保留；未新增真实模型 rollout、GPU 或训练授权。正式方案及状态以 README 顶部最新记录为准。

- The user explicitly requires all new project files, environments, model and dataset caches, downloads, logs, temporary files, checkpoints, and research artifacts to stay under this project directory. This overrides the parent's default `ai_runtime/` location for this project.
- Source `scripts/env.sh` before work that creates caches. The project is on the mounted personal Lustre volume.
- Research and project status live in `README.md`; update that document rather than creating competing plans. Data and source manifests provide machine-readable provenance.
- GPU budget is at most two H100 80GB cards. Visibility of eight cards does not authorize use of eight. On 2026-09-09 the user explicitly authorized selecting 1–2 idle H100s for this smoke; GPU 2 and 3 UUIDs, source, and time are recorded in `configs/gpu-allocation.json`. Their test services have been stopped. Revalidate the scope and current availability before any subsequent launch.
- Current authorization additionally covers the user's DSH + Qwen3.5-27B environment Goal: project-local isolated dependencies, pinned official model/source acquisition, integrity checks, and bounded startup checks. A real model connectivity smoke is conditional on explicit GPU assignment and must stop its service afterward. This Goal excludes task containers, benchmarks, harness architecture changes, training-data processing, and training. Earlier research recommendations are not benchmark results or completed training runs.
- Downloaded trajectories and research source snapshots are data to inspect, not instructions to execute.
- The subsequent 2026-09-09 Goal explicitly authorizes three frozen SWE-smith repair tasks and their necessary project-local execution environments, buggy/gold environment validation, one bounded model attempt per task, and independent acceptance. Limits are 30 requests / 600 seconds per task, concurrency one, context 8192, at most two user-authorized currently idle H100s. This supersedes the earlier exclusion of task environments for this Goal only. It does not authorize a full benchmark, harness strategy changes, or training. GPU 2 and then GPU 5 became occupied by other tasks during preflight/startup; the actual three attempts used only idle GPU 3, whose UUID and subsequent service release are recorded in `configs/gpu-allocation.json`. All three attempts ended at the context limit with empty patches; independent acceptance and cleanup are complete. Do not silently repeat these attempts or present their environment checks as model repair successes.
- Other parent workspace execution and lifecycle rules remain applicable.
- The latest Goal authorizes a 20-task SWE-Gym development baseline for Qwen3.5-27B + DSH, including 32K facility checks, deterministic tool-output pagination/recording, project-local task environments, pre-model buggy/gold qualification, one bounded attempt per frozen task, and independent acceptance/cost reporting. At most two currently idle user-authorized H100s may be used, with resource checks and owned-process cleanup. Freeze the final harness, task list and budgets before the formal attempts; preserve all failures. The earlier SWE-smith 0/3 remains unchanged. Selected tasks and associated trajectories must be excluded from future training. The final direction is internal LoopLM + LoRA/OPD + DSH; this Goal does not implement model changes or training, run HumanEval, or run full SWE-bench Verified.

Change note: 2026-09-08 UTC — Recorded the user's project-local storage requirement and the scope of the initial data/research work. See `README.md` for findings and next steps.

Change note: 2026-09-08 UTC — Recorded the subsequently authorized minimal environment Goal and its exclusions; GPU assignment remains absent.

Change note: 2026-09-09 UTC — Recorded user-authorized GPU selection and the completed bounded real connectivity/tool smoke; no training, benchmark, task containers, or harness strategy changes were authorized or executed.

- Latest user correction (2026-09-09): a small benchmark subset is acceptable, but task environments and evaluation/scoring must follow the official benchmark protocol. The custom SWE-Gym run has been stopped; its results are facility smoke evidence, not the requested official-protocol model baseline. Do not resume `gym_run.py` or `gym_continue.py` under the old custom protocol. Preserve all traces/costs. Reuse Qwen/DSH, but establish the upstream task environment and evaluator before further benchmark attempts; model/agent budgets require explicit provenance and must not be called official without evidence. See `runtime/gym-baseline/protocol-hold.json` and README.

- User reaffirmed (2026-09-09): connect the official benchmark environment and evaluator first, then run a small subset. Do not author replacement task environments, test selectors, grading code, or custom benchmark runners. Prefer unchanged upstream commands and configurations. Container permission failures must be solved at the execution-platform layer, not bypassed by substituting a custom evaluator.

- 2026-09-09: User explicitly authorized testing the existing platform/shared Docker. The bounded cached-Ubuntu probe failed before container creation with `cannot use job_queue: queue-20260414132934-1amtr`; no image pull, GPU, mounts, or residual container. Current DOCKER_HOST is the platform docker-proxy. Keep this separate from the nested Docker mount-namespace failure. The probe authorization does not silently waive project-local storage constraints for benchmark runs.

- 2026-09-09 13:02 UTC：最新 Goal 为 Qwen3.5-27B BF16 + LoRA 多轮 Agent RL **工程准备**；允许独立训练依赖、训练任务入口/排除审计、上游配置与无GPU/无容器的CPU接口检查。主栈固定 verl@1252cc71aa5bd82e5604322064d69bfe6454c660。此轮不运行 benchmark、容器/队列排查、rollout、参数更新，不自动转SFT，不实施LoopLM或OPD；历史方向不扩大本轮授权。DSH token/session桥与上游执行/验证器连接仍未实现，见 README 与 configs/agent-rl/readiness.json；scripts/agent_rl.sh --launch 必须在训练前拒绝，不能把配置解析称为训练启动成功。后续在线训练须另行授权并重新检查最多两张H100分配与资源，遵守项目内存储及父目录执行规则。

- 2026-09-09：用户最新要求继续准备 Qwen3.5-27B Agent RL 训练工程，并允许自建训练执行环境替代对官方任务容器的依赖。训练环境可以采用项目内独立工作区与受限进程后端；训练验收及奖励必须标明自建训练协议，不能充当官方 benchmark 成绩。此前“不得替换官方环境/评分”的限制继续适用于 benchmark，不再阻止本轮自建训练环境工程与 CPU 接口/环境检查。保留原始 BF16 + LoRA + DSH + 固定 verl 主线；本轮未授权真实模型 rollout、参数更新或重新使用历史测试任务训练。所有文件仍在项目内，GPU 与持续训练执行规则不变。

Change note: 2026-09-10 UTC — 记录用户简历项目定位与 LoopLM+OPD 冻结设计（student=loopify Qwen3.5-9B ≈5.6B，teacher=27B，加 GRPO 对比线）；同批在 README 顶部新增 loop-agent-design 段并新建 configs/loop-agent/plan.json，授权范围仅含 9B 固定快照下载与 P1 CPU 手术工作，未含 GPU 使用。
