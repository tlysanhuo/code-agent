接手 code-agent 项目（简历面试项目）。先读三个文件再动手：
/lustre/prod_glm_volumes/volume-20260201002229-o7c51/code-agent/ 下的
PROGRESS.md（进度单一入口）、docs/research-log.md（完整调研与决策）、
configs/loop-agent/plan.json（机器可读状态）。
纪律在 docs/workspace-discipline.md；结果证据在 reports/；GitHub =
tlysanhuo/tlysanhuo-code-agent-rl（作品集级 README，推送前过访客视角）。
source scripts/env.sh 后再跑任何东西。

【立即事项】阶段② GRPO 奖励设计（未完成，当前最高优先）：
- 用户裁定：奖励设计只认基模大厂 TR（DeepSeek-V4 / Qwen3-Coder-Next / GLM-5），
  Klear 等只当数据用不算配方权威；不许自创规则
- 三份 TR 全文快照：research/sources/base-model-trs-20260911/；
  奖励章节已提取为纯文本 tmp/{qwen3cn,glm5,dsv4}_tr.txt（含逐字引文：
  Qwen3CN 未完成惩罚+工具格式 token 级惩罚+防作弊拦截器；GLM-5 环境崩溃剔除+
  组填充/丢弃；DSV4 专家 GRPO+GRM→OPD 整合）——综合设计文档未写，
  先写完（research/ 下）并给用户过目再实现
- slime 原生机制已核实：remove_sample→零 loss_mask（=GLM-5 剔除/Klear 掩码通道）、
  token 级 loss_mask 原生、advantage 支持 grpo/gspo/cispo+custom 钩子

【在飞】全量 291 任务 qualification 批后台跑（nohup，logs/prepare-rl-round1-full.log，
产出 configs/agent-rl/local-task-registry.json + data/agent-rl/rl-round1-prompts.parquet；
试点合格率 20-24%）。

【已完成待续】阶段① SFT 验收全过（HumanEvalPlus 96.88% 持平基线、NLL 交叉验证、
A/B 解决率 15%→30%，见 reports/）；阶段②接线：LocalProcessSandbox（9/9 测试）、
本地 RL 后端（金标=1.0/空=0.0 集成测试过）、scripts/train_agent_rl.sh
（print-only；超参未获用户批准，--launch 必须拒绝）。

【硬纪律】调研先行（先看别人实现，不自写）；训练参数变更需用户同意；bench 全本地
（无 Docker/远端）；WANDB_MODE=online 不许改（key 在 configs/secrets/wandb.env）；
GPU 逐次授权记 UUID 进 configs/gpu-allocation.json、用完验证释放；官方 benchmark
只走官方协议；引用带标题+arXiv号+机构；没验证的不说完成；共享机 ray 独占端口
（20379/28266），清理只杀自己会话目录匹配的 PID；vendor/slime 零修改。

【下一步队列】① TR 调研成文→奖励栈实现（slime_dsh/ 内，带单测）→GRPO smoke
（超参先给用户批）② qualification 出数定首轮规模 ③ evalplus 官方 CLI 双模型
④ cudnn/torchaudio 升级 + iter_249/439（270G）清理确认 ⑤ LoopLM 探索臂最后。
